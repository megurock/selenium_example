extend ./partial/_layout
block body
  main
    form#search.pure-form.pure-form-aligned(action="/" method="post")
      fieldset 
        legend= title
        .pure-control-group
          label(for="username") ページID
          input(type="search" name="id")
        .pure-control-group
          label
          button.pure-button.pure-button-primary(type="submit") ページ検索
    script(src="js/search.js")